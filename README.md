# SIA MKAA — Construction site (Flask)

Local development:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5001

Notes:
Adding your uploaded image:
- Place the image you sent into `static/img/` and name it `sia_uploaded.png` (or `sia_uploaded.jpg`).
- The template will show it automatically in the hero column. If you want, I can add it for you if you upload the file into the workspace.
